<%= form_with(model: categoria_norma, local: true) do |form| %>
  <% if categoria_norma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoria_norma.errors.count, "error") %> prohibited this categoria_norma from being saved:</h2>

      <ul>
      <% categoria_norma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :categoria_norma_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
